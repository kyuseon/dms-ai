# AI Harness Engineering Rules

다중 AI(Sub-agents) 환경에서 시스템이 통제 불능 상태로 빠지지 않고, 예측 가능하고 안정적인 결과물을 내기 위한 가드레일(Guardrail) 및 규칙입니다. 모든 서브 에이전트와 PM은 다음 원칙을 반드시 준수하여 업무를 수행해야 합니다.

## 1. 사전 컨텍스트 주입 강제 (Context Management)
- **규칙**: AI 페르소나는 어떠한 작업을 시작하거나 코드를 수정하기 전, 임의로 파일 구조나 코드를 추측해서 작성하지 않는다.
- **실행**: PM이 지정한 관련된 문서와 주요 대상 파일들, 그리고 가장 기본이 되는 `0.prime.md`를 반드시 먼저 읽고(`view_file` 등 활용) 정확한 컨텍스트를 파악한 뒤에 코딩/설계를 진행해야 한다.

## 2. 에이전트 간 핸드오프(Handoff) 규격화
- **규칙**: PM과 서브 에이전트, 혹은 에이전트 간에 협업 및 인수인계를 할 때 전달되는 지시문은 명확한 템플릿 인터페이스를 가져야 한다.
- **실행**: 다른 페르소나에게 작업을 요청할 때는 다음 항목을 필수로 명시해야 한다.
  - `[목표]`: 달성해야 하는 명확한 목적
  - `[제약사항]`: 지켜야 할 기술적, 설계적 제약 사항 (권한 밖 수정 불가 등)
  - `[작업 대상]`: 수정 또는 열람해야 할 명확한 절대 경로 파일 목록
  - `[검증 방법]`: 작업 완료 후 에이전트가 스스로 성공 여부를 입증할 수 있는 테스트/실행 절차

## 3. 검증 우선주의 (Validation Guardrails)
- **규칙**: 코드를 눈으로만 작성 및 짐작하고 성공했다고 단정하여 섣불리 보고하거나 원본을 장악하지 않는다.
- **실행**: (예: C++ 개발 에이전트 등) 코드를 수정한 후에는 직접 컴파일(빌드), 스크립트 실행, 혹은 단위 테스트(Dry-run)를 반드시 돌려보고 구문 오류(Syntax error)나 논리 오류가 없음을 터미널 출력 등으로 증명한 뒤에 완료를 선언해야 한다.

## 4. 상태 및 사고 과정 추적 (State Tracking)
- **규칙**: 작업 중간에 AI가 무엇을 판단하고 어떤 계획을 세웠는지 인간 개발자(Human)가 투명하게 들여다볼 수 있어야 한다.
- **실행**: 여러 파일 변경이 수반되는 복잡한 문제 해결 시, 코드를 치기 전 미리 `task.md` 등에 자신의 상세 스텝과 계획을 기록(`Plan`)하고, 그 단계(체크리스트)에 맞추어 점진적으로 실행(`Execute`)한다.

## 5. 인지적 권한과 경계 준수 (Scope Limitation)
- **규칙**: 각 페르소나는 자신에게 할당된 도메인(영역) 밖의 시스템을 임의로 건드려서는 안 된다.
- **실행**: 예컨대, 설계(Architect) 에이전트가 직접 C++ 구현 코드를 뜯어고치거나, 개발(C++ Specialist) 에이전트가 DB 마이그레이션 스키마를 단독으로 수정하지 않는다. 다른 영역의 수정이 수반되어야 할 때는 즉각 해당 수정 사항을 PM에게 리포트하고 알맞은 페르소나에게 작업을 인계(Handoff)해야 한다.
